# Shared Schema Notes

This folder is reserved for future shared schema extraction.

For the current milestone:

- Four.Meme keeps using `schemas/strategy-spec.schema.json` as the canonical compatibility path.
- bStocks uses its own self-contained schemas under `schemas/bstocks/`.

This avoids breaking the existing Four.Meme flow while the repo grows a second strategy-skill lane.
