# Tests

Planned focus:

- schema validation
- rule-generation determinism
- rejection and invalidation edge cases

Favor focused tests that protect the strategy contract over broad framework noise.

Current smoke checks:

- `npm run test:bstocks-brain`
- `npm run test:fourmeme-brain`
- `npm run test:token-info`
