# Four.Meme Schema Notes

The current canonical Four.Meme schema remains:

- `schemas/strategy-spec.schema.json`

This folder exists to make the repo structure explicit now that bStocks has its own schema folder.

If the repo later extracts shared schema pieces under `schemas/common/`, the Four.Meme lane can move here without changing the current milestone behavior.
